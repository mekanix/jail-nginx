{% for item in nginx_consul_templates %}
template {
  source = "{{ consul_template_root }}/{{ nginx_consul_template_prefix }}/{{ item.filename }}"
  destination = "{{ item.destination }}"
  command = "service consul-template restart"
}
{% endfor %}
